name: Test Nxtstart Creation 👷
on:
  push:
  #schedule:
  #  - cron: 0 9 * * 1
jobs: 
  npm:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4
      - name: Setup Node.js 🔧
        uses: actions/setup-node@v4
        with:
          node-version: 20
          registry-url: 'https://registry.npmjs.org'
      # needed to avoid npm authentication problems in github action runner when trying to npm install
      - run: rm package-lock.json
      - run: mkdir nxtstart
      - run: shopt -s extglob dotglob
        working-directory: ./nxtstart
      - run: mv !(nxtstart) nxtstart
        working-directory: ./nxtstart
      - run: shopt -u dotglob
        working-directory: ./nxtstart
      - run: npm install
        working-directory: ./nxtstart
      - run: npm install -g
        working-directory: ./nxtstart
      - run: npm run testNpm
        working-directory: ./nxtstart-test
  yarn:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4
      - name: Setup Node.js 🔧
        uses: actions/setup-node@v4
        with:
          node-version: 20
          registry-url: 'https://registry.npmjs.org'
      # needed to avoid npm authentication problems in github action runner when trying to npm install
      - run: rm package-lock.json
      - run: mkdir nxtstart
      - run: shopt -s extglob dotglob
        working-directory: ./nxtstart
      - run: mv !(nxtstart) nxtstart
        working-directory: ./nxtstart
      - run: shopt -u dotglob
        working-directory: ./nxtstart
      - run: npm install
        working-directory: ./nxtstart
      - run: npm install -g
        working-directory: ./nxtstart
      - run: npm run testYarn
        working-directory: ./nxtstart-test